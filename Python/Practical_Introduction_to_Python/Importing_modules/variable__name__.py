# Import test_to_import file
import test_to_import

# When the interpreter runs a module, the
# __name__ variable will be set as __main__
# if the module that is being run is the main program

# Python file one module
print("variable name file __name__ is set to: {}".format(__name__))
